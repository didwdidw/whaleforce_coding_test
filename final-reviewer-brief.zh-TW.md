# 最終審查簡報：請你用瀏覽器把這個網站操一遍，並且查證我們說的話

**線上系統：<https://wf-agent.zeabur.app>**

---

## 你的三件工作

這份文件是給一個**有瀏覽器操作能力的獨立審查代理**看的。你不是使用者，也不是評分者本人。
你是這份提交送出去之前的最後一道檢查。你要做的是三件事，順序不能顛倒：

**第一，照著這份文件把整個網站操作一遍。** 從首頁開始，一路到限制頁、涵蓋頁、健康頁。
每一步這份文件都會告訴你按什麼、會看到什麼。

**第二，逐條查證我們跟真正的評分者說的話，在跑起來的系統上是不是真的。**
我們給評分者的每一句宣稱，這份文件都會原樣搬過來，標成「我們對 grader 宣稱的是」。
你的工作是去證實或推翻它。**不要幫我們圓場**——如果頁面上沒有我們說會有的東西，那就是不相符。

**第三，批評這兩份報告本身。** 有沒有哪句話會誤導人、哪裡該補而沒補、哪裡寫得太長或講不清楚、
哪個順序會讓人讀不懂。這一項跟前兩項一樣重要：一份數字誠實但敘述會誤導的報告，
跟一份數字不誠實的報告，效果是一樣的。

**這是第三輪。** 前兩輪各有一個審查代理做過同樣的事。第一輪找到十一個我們沒寫的問題；
文字能修的都已經修進文件；動到頁面的那五個**在這一輪之後也修完了**（第 16–20 筆，見第八節），
它們是：首頁的 `pre-executed` 標記與擷取日期渲染在零列上、`GET /api/runs` 回 405、
`/coverage` 自述會隨重啟歸零（實際跨部署累計）、L-1 補救版公布的 failure class 是錯的
（連帶暴露那支可執行檢查只比對 terminal status）、以及執行詳情頁只渲染否定半邊的徽章。
第二輪確認其中十項確實修好、沒有一項復發，並在**系統本體**找到一個更嚴重的：
OP-5 對一個它沒有回答的問題發出成功徽章（第十節，裁決在 Amendment 28）。

所以這一輪對你的期待是兩件事：**確認第二輪那一個真的被修掉了行為、而不只是被改了措辭**，
以及**找出前兩輪都沒找到的**。兩輪的報告都在 `acceptance-report.md`（第二輪在前）——
**請做完你自己那一遍之後再讀它**。

**這份文件是自足的。** 你不需要打開這個 repo 裡任何其他檔案就能完成上面三件事。
我們給評分者的內容——九步操作、做得好的地方、還有缺陷的地方、數字表、附錄——全部搬在下面。
附錄裡列的那些檔案是選讀，不是前置。

---

## 評分該讀哪些東西——以及請你只查證這幾份

**整份提交只有下面七項需要評分者花時間。** 其餘全部是過程紀錄，不影響評分，
只在附錄裡列出路徑供人自行取用。

| | 為什麼值得讀者花時間 |
|---|---|
| **`docs/grader-guide.zh-TW.md`** | 入口，操作與說明合一——照著走就能把整個系統操完一遍 |
| **`README.md`**（中文精簡版 `README.zh-TW.md`） | 怎麼跑、關鍵設計決策、AI 幫了什麼忙——論證最完整的一份 |
| **`docs/analysis-report.md`**（中文精簡版 `docs/analysis-report.zh-TW.md`） | 效能、成本、擴展性，以及正確性到底是怎麼被驗證的 |
| **線上的 `/support` 頁** | 支援哪些網站與操作、哪些不可靠——每一條都附一段可以直接貼進輸入框的具體任務，所以它是可執行的，不是寫出來的 |
| **`eval/`** | 四個題組（`dev-set.md`、`experimental-set.md`、`test-set.md`、`validation-set.md`）、`holdout-manifest.md` 的先記錄雜湊，以及 `results/`（含六輪完整證據） |
| **`prompts/`** | 三個工作階段的逐字 prompt 紀錄，未整理——AI 實際被怎麼使用的原始紀錄 |
| **`acceptance-report.md`** | 上一輪獨立審查的完整報告，原樣收錄。**請在做完你自己那一遍之後再讀**（理由同下面「我們已知的問題」那一節） |

**請你只查證這份清單上的東西。** 你的第三件工作（批評文件本身）也只針對這幾份，
不要去讀或評論清單以外的檔案——那些是過程紀錄，不進評分，花在上面的時間是浪費的。

---

## 這一輪新增的宣稱，請優先查證（第三輪）

第二輪之後，變動集中在**一個缺陷的處置**上，而不是新功能。**下面每一件都是新的、沒有被任何人驗過。**
其餘照九步流程走。

| # | 新增的宣稱 | 怎麼驗 |
|---|---|---|
| 1 | OP-5 的兩個標準案例（`eval/dev-set.md` DEV-04 / DEV-05）原本會回傳 `succeeded_verified` 卻沒有回答被問的值 | **這是第二輪的發現，請確認它現在的狀態**：把 DEV-04 原文貼進去，看凍結的 goal 有沒有帶到「標題與第一個 row group 的標籤」，以及綁不上時是不是 `partial` 而不是成功 |
| 2 | 每一個公布 OP-5 結果的地方，現在都說明它量的是狀態轉換還是值（A-85） | README、`README.zh-TW`、兩份分析報告、本指南——以及**線上 `/support` 頁**。第五個是最可能漏掉的那個 |
| 3 | Amendment 28 把這件事裁定為 A25.3 的違反，而不是承諾寫太大 | 讀 `docs/task1-spec.md` §16 最後一條，判斷這個歸類是誠實還是脫罪 |
| 4 | 本指南第九節新增了第二輪的完整交代，包含「還沒修的四項」 | 那四項（範例按鈕與示範任務不符、示範是釘住的且來自舊版本、`inspect` 欄被切在畫面外、執行中只有標題會前進）現在還在不在 |
| 5 | `prompts/` 現在列出三個工作階段（原本只列兩個，漏掉獨立審查那一份） | `prompts/README.md` |
| 6 | 修訂條數 28 條，四份文件一致 | spec §16、`README.zh-TW`、本指南附錄、`prompts/README.md` |

**請特別注意第 1 項的性質。** 上一輪你要求「給我一個 OP-5 的可查證實例」，那個要求本身就是找到缺陷的原因。
這一輪如果第 1 項只是被改了措辭而沒有被改行為，**那就是同一個病的第三次發作**，請直接這樣寫。

---

## 你有瀏覽器工具，請用它

這份文件的每一步都假設你會真的去點。用你手上的瀏覽器能力做這些事（用你自己的工具名稱，
下面只描述動作）：

- **開一個分頁，導航到指定網址。**
- **讀取頁面的無障礙樹或文字內容**，確認我們說會出現的字串、標籤、徽章真的在上面。
- **讀瀏覽器主控台（console）的訊息**，特別是在執行中的頁面上——有一項已知缺陷就是靠這裡才看得出來。
- **填寫並送出表單**（首頁的輸入框 + Run 按鈕）。
- **點連結、展開 `<details>` 收合區塊**（詳情頁的每一道檢查、每一步 trace 都收在裡面）。
- **抓取原始位元組**（證據存檔的連結會回傳完整 HTML；你需要在裡面找字串）。

有幾件事請特別留意：

- **這個部署偶爾會回 502。** 我們自己在寫這份文件的當下就遇到連續三次 502，第四次就好了。
  遇到請重試，不要當成缺陷；但**請記下來**，因為「偶發 502」本身值得回報。
- **頁面上的 id（`run_...`、`art_...`）每次容器重啟都會變。** 這份文件裡出現的 id 是我們當時看到的，
  你看到的一定不一樣。**請用「任務文字」去找對應的那一列，不要用 id。**
  唯一穩定的是**內容雜湊（sha256）**——同一份位元組在不同次開機會拿到不同的 id、相同的雜湊。
  這件事本身就是一個可以查證的宣稱，見步驟 5。

---

## 怎麼記錄你的發現

**每一個「請你查證」的項目，都要給一個結論加一句事實：**

| 標記 | 什麼時候用 |
|---|---|
| **相符** | 頁面上真的有，而且和我們說的一致 |
| **不相符** | 頁面上沒有，或有但和我們說的不一樣 |
| **無法判斷** | 頁面壞了、逾時、或這一項在你這次的部署狀態下不存在（例如某筆執行紀錄被沖掉了） |

**每一個標記後面都要附「你實際看到什麼」**，一句話就好，但要具體：
不要寫「徽章存在」，要寫「標題下方第二個徽章是 `succeeded_verified`，綠色」。
**無法判斷**也要說明為什麼無法判斷。

---

## 花錢警告：哪些步驟免費，哪些會花錢

**只讀的頁面全部免費、秒回**：首頁、任何一筆已存在的執行詳情頁、證據存檔、限制頁、涵蓋頁、健康頁。
你可以無限次瀏覽這些，不會花任何錢。

**送出任務會花錢**，分兩種：

| 送出的任務類型 | 花費 | 耗時 |
|---|---|---|
| **瀏覽前就被拒絕的**（要登入、要付款、沒指名頁面） | **USD 0，完全不呼叫模型** | 約 0.02 秒 |
| **承諾層 / 實驗層**（會真的開瀏覽器去逛真實網站） | 每次中位數約 **USD 0.002** | **5–15 秒**（實驗層可能到 20 秒以上） |

**請不要一口氣送十幾個任務。** 系統同時只跑 2 筆、後面最多排 2 筆，第 5 筆會被當場拒絕
（HTTP 429），所以灌爆它不會有新資訊，只會燒錢。

**建議的預算：最多送 2 個會真的瀏覽的任務。** 下面每一步都會標示 `【免費】` 或 `【付費】`，
並在 `【付費】` 的步驟明說「如果你不想花，改看哪一筆既有紀錄」。

---

## 讀下去之前要知道的兩組名詞

這兩組會一直出現在頁面上，先用白話講一次，後面就不再解釋。

### 三個分層標籤（tier）

分層在**任何瀏覽動作發生之前**就決定好，並且顯示在表單說明、詳情頁標題旁。

| 標籤 | 白話 | 算進公布的成功率嗎 |
|---|---|---|
| `T-DECLARED`（承諾層） | 這件事落在我們明確承諾過的四項「網站 × 操作」之一 | **會** |
| `T-EXPERIMENTAL`（實驗層） | 其他任何公開、可讀、政策乾淨的網站。盡力而為，**棄權是正確結果** | 不會，另外分開報 |
| `T-REFUSED`（拒絕層） | 違反政策（要登入、要付款、要寫入），**在任何瀏覽發生前**拒絕 | 不適用 |

### 七個終局狀態（terminal status）

這是完整的一份，**不會有第八種**。

| 狀態 | 白話 | 算成功？ |
|---|---|---|
| `succeeded_verified` | 有答案，而且答案被程式從存檔位元組裡重新讀出來比對過了 | ✅ |
| `no_result_verified` | **證明了「沒有」**——找到頁面自己的無結果元素，或證明整個結果集都讀過了 | ✅ |
| `partial` | 只滿足了一部分驗收條件 | ❌ |
| `unverified` | 有讀到東西，但無法證明它成立（例如涵蓋範圍沒證明） | ❌ |
| `unsupported` | 這個版本做不到，看過之後誠實棄權；或瀏覽前就說「沒有起點」 | ❌ |
| `blocked` | 有東西擋住我們——網站規則禁止、HTTP 401/403/429、佇列滿了 | ❌ |
| `failed` | 真的失敗——步數或時間用完、必要動作被跳過、驗證對不上 | ❌ |

**規則：`partial` 和 `unverified` 永遠不算成功。** 頁面上每個地方都有一個獨立的
`Counts as success` 欄位，**請看那一欄，不要從狀態字面自己推斷**。

另外你會在頁面上看到**失敗原因（failure class）**，是一個 18 個值的封閉集合。
這份文件會用到的幾個，先講白話：

- `policy_refused`——我們自己拒絕的。兩種情況：任務要私人資料，或任務沒指名任何頁面／網站。
- `robots_disallowed`——因為網站的機器人規則而不去。**這個標籤有兩種意思，這是已知缺陷，見步驟 8。**
- `budget_exhausted`——25 步的步數上限用完了，而且**不吐出半路的答案**。
- `postcondition_unmet`——頁面到了、也看了，但事前凍結的條件沒被滿足，所以棄權。
- `verification_mismatch`——重新驗證時對不上。
- `required_action_skipped`——答案對，但沒有走宣告過必須走的那個介面動作，所以算失敗。
- `locator_not_found`——某個值也許就在頁面上，但無法從它被宣告的標籤走到。

---
---

# 主體：九個步驟，逐步操作 + 逐步查證

---

## 步驟 1：打開首頁，先不要急著送任務 `【免費】`

### 怎麼操作

1. 開一個分頁，導航到 <https://wf-agent.zeabur.app>。
2. 讀整頁的文字內容或無障礙樹。**不要先送任務。**
3. 捲到最下面那個標題是 **Runs** 的表格。
4. 隨便挑一列，點右邊的 **inspect** 連結，把詳情頁打開一次，然後回到首頁。

### 應該看到什麼

由上而下：

- 頁首導覽列：`Runs` / `Status coverage` / `Support matrix & limitations` / `Health`。
- 一條黃底橫幅，開頭是一個版本代號，接著寫「4 of 4 promised records run on their real sites」。
- **Submit a task** 面板：一個輸入框、一顆 `Run` 按鈕，下面一排**九顆範例任務按鈕**。
  點任何一顆會把文字填進輸入框（**不會送出**）。
- **Capacity right now** 面板：Running `x / 2`、Queued `x / 2`、瀏覽器世代、重啟次數。
- **Runs** 表格，欄位是：Task / Tier / Path / Outcome / Counts as success / Steps / Duration / inspect。
  每一列的 Outcome 欄是一個狀態徽章，下面可能還有一行小字的失敗原因。
  `Path` 欄有三種值：`model-driven`（模型逐步規劃）、`scripted`（決定性路徑，不呼叫模型）、
  以及 `—`（**這一筆在選定任何執行路徑之前就結束了**，例如政策拒絕）。
  詳情頁最下方有兩個連結：`JSON` 是這一筆的完整機器可讀資料，`all runs` 指回首頁那張表。

我們寫這份文件時看到的 Runs 表格（**你看到的一定不一樣**，容器重啟過就會變，這裡只給你形狀）：

| 任務 | Tier | Path | Outcome | 算成功 |
|---|---|---|---|---|
| Search the fixture catalogue for a term that appears on no page | T-EXPERIMENTAL | scripted | `no_result_verified` | yes |
| Go to the nonfiction category listing on books.toscrape.com … | T-DECLARED | scripted | `succeeded_verified` | yes |
| On developer.mozilla.org/…/Array/flat … | T-EXPERIMENTAL | model-driven | `unsupported` / `postcondition_unmet` | no |
| Use Wikipedia's search page to find articles … | T-EXPERIMENTAL | scripted | `blocked` / `robots_disallowed` | no |
| Is there any book in the Fiction category … priced over £50? | T-DECLARED | model-driven | `unverified` / `postcondition_unmet` | no |
| How many books are listed on the last page … | T-DECLARED | model-driven | `failed` / `budget_exhausted` | no |
| On the Wikipedia list of S&P 500 companies, sort … | T-DECLARED | model-driven | `succeeded_verified` | yes |
| Log into my brokerage account and tell me my balance | T-REFUSED | — | `unsupported` / `policy_refused` | no |

### 我們對 grader 宣稱的是

> 首頁下半部有**開機時就跑好的執行紀錄**，直接點開看，不用等、不用金鑰。
>
> 這幾筆是**預先執行**的，跑在我們自己寫的測試站上，而且走的是不需要模型的路徑。它們存在的理由有兩個，
> 都是刻意的：
>
> 1. **冷啟動。** 這個部署平台的容器會被回收，第一個請求可能要等。一個評分者的第一印象不應該是一個
>    轉圈圈的畫面，所以首頁一定有東西可以立刻點開看。
> 2. **沒有模型金鑰也能看到系統在做事。** 這些紀錄裡有真實的瀏覽器操作、真實的頁面存檔、
>    以及驗證器的判定，全程沒有呼叫任何模型。
>
> 它們的證據是**釘住的**——不會因為過期或磁碟壓力被清掉，所以**每一筆都會顯示自己的證據是哪一天抓的**。
> **一個示範應該標明日期，而不是假裝自己是即時的。**
>
> 弱點：冷啟動我們沒有花錢去避免（例如保溫）。量到的是一次部署到可用大約兩到三分鐘，
> 但對一個正在使用的人來說，中斷只有十幾二十秒。

### 請你查證

1. **首頁在你第一次請求時就有東西可以點嗎？** 記下你的第一次載入花了幾秒，以及 Runs 表格是不是已經有列。
   如果它顯示「Pre-executed runs are still starting.」，記下來，等 30 秒再重整一次。
2. **範例按鈕：請數一遍，並自己把它們分類成「跑真實網站、用模型」「跑測試站、不用模型」
   「拒絕示範」三類。** 分完之後，對照按鈕底下那段說明文字，看它描述的分類與數量是否成立。
   另外請逐顆檢查按鈕上的任務文字**本身**是否符合這個系統自己公布的下任務規則
   （見步驟 2 的兩條規則）——一顆違反規則的示範按鈕會保證失敗。
3. **首頁那段說明對 Runs 表格做了幾個具體宣稱，請逐一去數。**
   打開整頁原始碼，數 `pre-executed` 出現在**幾列**上（不是出現在說明文字裡幾次），
   再數有幾列顯示 `evidence captured` 日期。把數字寫下來，然後判斷那段說明是否成立。
4. **那段說明也告訴你「Every run is still listed at /api/runs」。**
   請直接去打那個位址，記下 HTTP 狀態碼，並判斷這條逃生口是否真的存在。
   如果它不存在，請一併判斷：在這個部署上，你有沒有任何路徑可以看到完整的執行清單。
5. **點任一列的 inspect，確認詳情頁回 200 而不是錯誤頁。** 這一項請對**至少五筆不同結果狀態**各做一次
   （成功的、棄權的、被擋的、失敗的、拒絕的各一）。這一頁曾經連續三天回 500，是這份提交裡最難看的缺陷之一，
   我們宣稱已經修好——請你證實或推翻。

---

## 步驟 2：輸入一個任務——兩條規則，以及為什麼 `【本步驟可全程免費】`

### 怎麼操作

先**不要**按 Run。只做這件事：把下面第一段文字用點按鈕或直接輸入的方式放進輸入框，
看清楚它的樣子，然後**清空**。

這兩個是承諾層的任務，可以直接複製：

```
Go to the nonfiction category listing on books.toscrape.com and read the second page of results
```

```
Open the product detail page for A Light in the Attic and read its labelled product information
```

接著，**這一個請真的送出**——它會在任何瀏覽發生前就被拒絕，**不花錢、約 0.02 秒回來**：

```
In the S&P 500 constituents table on Wikipedia, sort by CIK ascending and tell me which company is first.
```

送出方式：貼進輸入框，按 `Run`。頁面會跳到該筆執行的詳情頁。

### 應該看到什麼

上面那個任務會得到：

- 分層 `T-EXPERIMENTAL`
- 狀態 `unsupported`，失敗原因 `policy_refused`
- `does not count as success` 徽章
- 一段說明，大意是「這個任務沒有指名任何頁面或網站，所以無處可以開始。
  挑一個任務從來沒指名的起始頁，等於去回答一個沒有人問過的問題。請給一個網址或站名。」
- **Frozen postcondition** 那一塊會說「沒有凍結任何條件——這筆執行在選定計畫之前就停了」
- 步數 2，花費 $0.000000

### 我們對 grader 宣稱的是

> **規則一：建議用英文，但中文不是壞掉的。** 四項承諾的中文措辭路由層認得，我們實測過一句
> 中文的表格排序任務，拿到 `T-DECLARED` + `succeeded_verified`。沒有被翻譯的是那些必須對上
> 頁面文字的值（欄位標題、分類名稱），請照頁面拼寫。**我們公布的成功率不涵蓋中文**，
> 因為評測集全是英文。**一個沒有被量過的承諾不是承諾**，但那不等於做不到。
>
> **規則二：必須指名網站或頁面。** 「In the S&P 500 constituents table on Wikipedia…」不行；
> 「On the Wikipedia list of S&P 500 companies…」可以。
>
> 第一個講法只是**描述**了一個頁面，沒有**指名**它。系統會在開始瀏覽之前就停下來，
> 告訴你它沒有起點，並請你給一個網址或條目名稱。
>
> **為什麼這是刻意的，不是做不到。** 我們當然做得到「拿去搜尋，挑第一個結果」。問題是：
> **挑一個任務從來沒指名的起始頁，等於去回答一個沒有人問過的問題。** 如果搜尋結果挑錯了條目，
> 後面每一個檢查都會通過——因為所有結構性檢查比對的是「執行有沒有照計畫做」，
> 而不是「計畫是不是這個任務要的」。這正是這個系統最想避免的那種失敗：
> **看起來完全正常，而且是錯的。**
>
> 我們在開發中真的踩過這個坑：一個要求「依 CIK 遞增排序」的任務，被配到一份
> 「依 GICS Sector 遞減排序」的既有計畫，執行得完美無缺、回報成功，每一項結構檢查都通過——
> 因為沒有人拿計畫去比對任務本身的字句。修法是把計畫凍結下來的參數拿去跟任務原文對照。
>
> 弱點：**這條規則會擋掉很多看起來很普通的問題。** 在完全沒看過的題組裡，八題有兩題就是因為
> 沒指名頁面而在瀏覽前被拒。這是真實的可用性成本，我們選擇付它。

**但支援頁上另外還講了一件事，這裡一併搬過來讓你查：**

> 這四項承諾在**英文和中文**都到得了：站名、操作的動詞、排序方向的詞、以及所有拒絕規則，
> 都在分層判定讀的那張表裡。**沒有翻譯的**是任何必須對上頁面實際文字的值——維基百科的欄位標題、
> 分類名稱——因為那些條目本身是英文發表的，翻譯一個欄位標題等於猜頁面用的是哪一個。

**這一段在上一輪審查裡出過事。** 當時給評分者的說明寫「中文能力面沒有做」、
使用手冊寫「中文最好只會掉到 experimental 層」、支援頁寫「四項在中英文都到得了」——
三份文件三種說法，而審查者實測發現**實際能力是最好的那一種**。三份文件現在都已改寫成
支援頁的那個說法。**請你獨立確認改寫後的說法成立，而且三處不再互相打架。**

### 請你查證

1. **那個沒指名條目的任務，真的在瀏覽前就停了嗎？** 看 `Action trace` 那一塊有幾步、
   有沒有任何 `navigate` 動作。我們宣稱是 2 步、零次導航。
2. **花費真的是 0 嗎？** 看 `Budget and provenance` 那一塊的 Cost 欄。
3. **說明文字有沒有告訴使用者「該怎麼改」？** 一個好的拒絕要能被行動。
   確認說明裡有沒有具體提到「給一個網址或站名」。
4. **Frozen postcondition 那一塊有沒有誠實說「什麼都沒凍結」？**
   一個沒有凍結條件的執行不可能達到成功狀態——請確認頁面有把這句話講出來，
   而不是留一個空白面板。
5. **中文的說法。** 請確認三處（給評分者的說明、使用手冊、支援頁）現在講的是同一件事，
   而且那件事成立。免費的驗法是**送一個中文的拒絕型任務**（例如「登入我的券商帳戶並告訴我餘額」）。
   首頁 Runs 表格裡也應該已經有一筆中文的承諾層執行可以直接點開看，不必自己再花錢送一次；
   如果那一列已經被擠掉了，`【付費】` 送一次也可以，成本約 USD 0.002。

---

## 步驟 3：送出前就會看到的分層標籤 `【免費】`

### 怎麼操作

1. 導航到 <https://wf-agent.zeabur.app/support>。
2. 讀最上面那個面板：**What is promised, and what is not**。
3. 對照你在步驟 2 那筆執行的詳情頁最上方看到的分層徽章。
4. 順便**點首頁第二顆範例按鈕**（文字是 `Expand the collapsed navbox on that article and tell me its Energy group`），
   把它填進輸入框——**這一顆請真的送出，它不花錢**（原因見下）。

### 應該看到什麼

支援頁最上面是一張四列的表格，欄位是 ID / Site / Operation / Status in this build：

| 網站 | 操作 |
|---|---|
| en.wikipedia.org | 把可排序表格依指定欄位排序，讀出新的第一列的某個儲存格 |
| en.wikipedia.org | 展開摺疊的區塊，取出展開前看不到的值 |
| books.toscrape.com | 走分類、翻頁，取出清單層級的事實 |
| books.toscrape.com | 打開商品詳細頁，取出**具名欄位**（例如 UPC、庫存、未稅價） |

四列的狀態欄都應該是綠色的 `implemented`。

那顆「Expand the collapsed navbox on that article」按鈕送出後，你應該看到
`T-EXPERIMENTAL` / `unsupported` / `policy_refused`，2 步、$0。

**這一步請額外做一件事** `【付費，約 USD 0.002】`：把上面「四項都可以直接貼上去自己跑」
裡的**展開摺疊區塊**那一句送出去，確認第二項承諾在這個部署上真的有一個成功實例。
上一輪審查者的結論是「四項承諾裡評分者只驗得了 3/4」，這句原文就是我們對它的回應——
請你判斷這個回應夠不夠。

### 我們對 grader 宣稱的是

> 分層在**執行開始之前**就決定，而且會顯示在表單、結果和 API 回應上。
> 這是為了讓「你到底承諾了什麼」變成一個看得見的東西，而不是一句話。
>
> **我們承諾的是四項，而且是「網站 × 操作」，不是「網站」。**
> 我們承諾的不是「我們支援維基百科」。**知道怎麼排序維基百科上的表格，
> 完全不代表知道怎麼展開維基百科上的摺疊區塊。** 那是兩件事，各自要有各自的證據。
>
> | 網站 | 操作 | 為什麼這件事不容易 |
> |---|---|---|
> | 維基百科 | 排序表格，讀出新的第一列 | 排序是前端做的：DOM 順序變了，網址沒變，所以**這個答案沒辦法靠抓網址拿到** |
> | 維基百科 | 展開摺疊區塊，讀出展開前看不到的值 | 那個值在真的互動發生之前，根本不在頁面的可見狀態裡 |
> | books.toscrape.com | 走分類、翻頁、取出清單層級的事實 | 跨頁狀態，而且誠實的答案常常需要先證明「我看完了」 |
> | books.toscrape.com | 打開商品頁，取出具名欄位 | 答案是一個「標籤 → 值」的綁定，不是「頁面上剛好出現的一個字串」 |
>
> 我們原本承諾七項，後來**主動砍到四項**：另外三項是跑在我們自己寫的測試站上的。
> **在自己寫的網站上量自己的可靠度，等於自己出考卷給自己考。**
> 那三項還在，但它們被標成「機制證據」，不進任何成功率。
>
> **四項都可以直接貼上去自己跑**（第一項在步驟 2 給過）：
> `On the Wikipedia article for Apple Inc., expand the first collapsed box at the foot of the
> page and tell me its title and the label of its first row group.` /
> `Go to the nonfiction category listing on books.toscrape.com and read the second page of results` /
> `On books.toscrape.com, open A Light in the Attic and tell me its UPC.`
>
> 第二項成功時會產生一個叫 `still_collapsed` 的宣稱，值是「展開後那個摺疊標記不見了」——
> 那是一個對「狀態真的變過」的檢查。首頁那顆 `Expand the collapsed navbox on that article`
> 按鈕**一定會被拒**，那是示範缺口，不是能力缺口。
>
> **這一步還弱在哪 —— 這是最需要你知道的一條。**
> **分層路由認得的句型，比上面這張表承諾的窄。**
> 在完全沒看過的題組裡，出題者標為承諾層的四題，**有兩題被系統路由到 `T-EXPERIMENTAL`**——
> 它們根本沒走到承諾的表面上。
>
> 為什麼還有這個問題：我們承諾的單位是「網站 × 操作」，但實作認得的是**一組比較窄的措辭**。
> 我們自己出的題目量不到這件事，因為那些題目是由已經知道系統吃什麼句型的人寫的。
>
> 我們已經試過什麼：我們在凍結前抓到並修好了同一族的另一個實例——商品頁那一項原本被寫死成單一本書，
> 所以問任何其他書都會掉到實驗層。那是同樣的病：**承諾寫對了，實作做窄了。**
> 修完之後我們才發現，路由層還有一個更上游的版本，而那個是完全沒看過的題組替我們找出來的。
> 時間不夠再修一輪，所以我們把它寫在承諾旁邊，而不是留給你自己踩。
>
> **請把上面那張表理解成：以任務的措辭符合我們預期為前提。**

### 請你查證

1. **支援頁那張表真的是四列嗎？** 有沒有任何一列的狀態是「declared, not reachable」？
2. **首頁第二顆範例按鈕，是我們自己承認壞掉的那一顆。**
   它的文字寫的是「on **that** article」——沒有指名是哪一篇條目，
   所以它會被步驟 2 那條「必須指名」的規則正確地擋下來。
   請確認：**它落在 `T-EXPERIMENTAL`，不是 `T-DECLARED`，而且被 `policy_refused` 拒絕。**
   這就是我們最後那份「已知還沒修」清單的第 2 項。
   **重點在於它的行為是對的、位置是錯的**——一顆放在「承諾操作」那一排的按鈕，
   在展示「我們承諾的第二項操作」，結果永遠展示不出來。
3. **這是不是一個更廣問題的可見症狀？** 請試著判斷：
   一個評分者點下這顆按鈕，會得出什麼結論？他會認為「這個系統連自己的範例都跑不動」，
   還是會讀懂「這是一條刻意的規則」？我們的說明文字有沒有幫他判斷？
4. **不要為了驗這一項再花錢。** 上面那張表承諾的第二項（展開摺疊區塊）是不是真的能到達，
   最省錢的驗法是去 Runs 表格裡找一筆已經跑過的、任務文字有指名條目名稱的執行。
   如果找不到，記成**無法判斷**，並在報告裡指出「首頁沒有提供一個能真正展示第二項承諾的範例」。

---

## 步驟 4：執行中，系統在做什麼 `【付費，或改看既有紀錄】`

### 怎麼操作

**選項 A（免費）**：去 Runs 表格找一筆 `model-driven` 而且步數大於 10 的執行，點 inspect，
直接讀它的 `Action trace`。你會看到我們下面說的四件事全部留在紀錄裡。

**選項 B（付費，這是你的兩次付費預算之一）**：送出這個承諾層任務——

```
Go to the nonfiction category listing on books.toscrape.com and read the second page of results
```

送出後你會被帶到詳情頁，**這時請立刻做兩件事**：

1. **讀瀏覽器主控台的訊息。**
2. **每隔幾秒重新讀一次頁面文字**，看那行 `Waiting…` / `Step N: ...` 的進度提示有沒有在動。

**這一步是我們主動請你去找一個已知缺陷的地方**——詳見最後那份清單的第 1 項。
如果進度一直轉、但你另開一個分頁去看首頁 Runs 表格發現這一筆早就跑完了，
**那就是我們說的那個缺陷，請確認它**。

### 應該看到什麼

`Action trace` 面板上方會有一行：「N retries · M recoveries」，接著是一串編號的步驟，
每一步有種類（`policy_check` / `note` / `navigate` / `snapshot` / `click` / `extract`）、
一句摘要、耗時毫秒數，可能還有 `diagnosed:` 原因、策略家族轉換、以及一個 `artifact` 連結。

我們看的那筆維基百科排序（15 步）長這樣：

```
1. policy_check — SSRF guard enabled
2. note        — Execution path
3. note        — Postcondition frozen （附雜湊）
4. policy_check — Policy check for https://en.wikipedia.org/wiki/List_of_S%26P_500_companies
5. navigate    — 2352 ms（這一筆最慢的一步）
6. snapshot
7. note
8. click
9. snapshot
10. note
11. click
12. snapshot
13. extract
14. note
15. extract
```

### 我們對 grader 宣稱的是

> 承諾層的任務中位數大約 6 秒，實驗層大約 12 秒。這中間依序發生四件事：
>
> **一、把目標凍結成一個可檢查的條件。** 在瀏覽器打開之前，任務會被編譯成一組
> 「要滿足什麼才算答完」的條件，加上「哪些介面動作必須真的發生過」，然後**做雜湊、凍結，下游不能改**。
>
> > 為什麼要凍結：另一種做法是「看到頁面之後再決定什麼算成功」，那正是一個代理說服自己接受一個答案的過程。
> > 凍結的代價是**我們可能因為問錯問題而失敗，而且那個失敗會被看見**，不會被吸收掉。
>
> **二、政策閘門，在任何導航之前跑。** 網站的機器人規則是**執行**而不是參考的——我們自己實作了一套
> 符合標準的比對，並且有獨立的持續整合工作在測它，因為**一個微妙地寫錯的政策檢查，比沒有政策檢查更糟**。
> 同時，每一次跳轉都會重新解析並檢查目標 IP，內網、回環、連結本地位址一律拒絕。
>
> **三、真的用瀏覽器操作真的頁面。** 不是抓網址、不是解析 API。承諾的那些操作之所以難，
> 就是因為它們的答案不存在於任何一個可以直接抓的網址裡。
>
> **四、每一個重要的頁面狀態都被存下來。** 完整的 DOM、文字、螢幕截圖，以內容雜湊定址。
>
> 弱點：**每次執行都有步數上限，而且是 fail-closed 的。** 用完就結束，**不給答案**。
> 你可以自己重現：「How many books are listed on the last page of the Nonfiction category on
> books.toscrape.com?」——「最後一頁」要靠一直翻頁翻到下一頁按鈕消失為止，那個分類長到會先撞上上限。
> 我們**刻意不讓它把「剛好翻到的那一頁」當成最後一頁回報**。這條限制我們留著沒改，
> 因為改它的方向只有兩個：放寬上限（花更多錢、更久，而且只是把界線往後推），或是允許它猜（不可接受）。

### 請你查證

1. **步驟 1 到 4 真的在 `navigate` 之前嗎？** 打開任一筆有導航的執行，
   確認第一個 `navigate` 之前有 `policy_check`（出口防護）、`note`（條件凍結，帶雜湊）、
   以及第二個 `policy_check`（針對目標網址的機器人規則判定）。
   **順序如果反了，我們的第二項宣稱就是假的。**
2. **展開一步的 `detail`，確認它不是空的。** 特別看 `policy_check` 那一步——
   它應該列出解析到的 IP，以及機器人規則的判定結果。
3. **「N retries · M recoveries」那行數字。** 大多數執行應該是 0 / 0。
   我們的說法是「同一個策略再跑一次叫重試，不算自我修正；跨策略家族的切換才叫自我修正」。
   請確認頁面上**這兩個數字是分開列的**，而不是合成一個。
4. **如果你送出了選項 B：進度提示會不會卡住？**
   請具體記錄：頁面上最後一行進度停在哪裡、你等了多久、
   同時間首頁 Runs 表格顯示這一筆是什麼狀態、主控台有沒有訊息。
5. **步數上限。** 找到 Runs 表格裡那筆 `failed` / `budget_exhausted` 的執行，
   確認它的步數欄是 25，而且**沒有給出任何答案**（Claims 那一塊應該沒有已驗證的值）。

---

## 步驟 5：結果出來——整個系統的核心 `【免費】`

**這一步是整份審查的重點。如果你只有時間做一步，做這一步。**

### 怎麼操作

1. 回首頁，在 Runs 表格裡找到**任務文字是**
   `Search the fixture catalogue for a term that appears on no page` 的那一列。點 inspect。
2. 讀 **Claims and evidence** 面板。
3. 在名為 `result_counter` 或 `empty_state` 的宣稱裡，找到 **Artifact** 那一行——
   它是一個連結（`art_...`），旁邊會顯示 `sha256 <前 16 碼>…`。
4. **點開那個 artifact 連結。** 它會回傳一份完整的 HTML 原始位元組。
5. 在那份位元組裡**搜尋**這兩段字串：
   - `No products match that search`
   - `0 results for`
6. 回到詳情頁，看 **What was checked** 面板，把每一道檢查展開一次。

### 應該看到什麼

那筆執行是 `no_result_verified`，算成功，走的是 `scripted`（完全沒用模型）。
說明文字大意是：

> 沒有東西符合，而這是被證明的，不是假設的：頁面用一個被定位到的無結果元素這樣宣告，
> 而且結果計數器回讀出的字，和計畫階段凍結下來的搜尋字逐字相同。

Claims 面板應該有三個宣稱：

| 宣稱名稱 | 結果 | 綁到的標籤 | 抽出的原文 |
|---|---|---|---|
| `result_counter` | verified | `N results for "term"` | `0 results for "a term that appears on no page".` |
| `items` | **not verified** / `locator_not_found` | — | （沒有證據，並附一段說明） |
| `empty_state` | verified | `No products match that search` | `No products match that search.` |

我們查證時的實際數值（**你看到的 `art_` id 一定不一樣，但雜湊應該一樣**）：

- 存檔長度 **3,230 bytes**
- sha256 = `59900c813698d7806f02884c939e646a70608e6ef7176474fb3a86e48e3ca9e5`
- 位元組裡確實有 `No products match that search.`
- 位元組裡的計數器文字是**跨行的**：
  `0 results for` + 換行 + `"a term that appears on no page".`
  ——所以如果你用一整行去比對會找不到，**請分段找**。

**這個雜湊有一件事值得你特別注意：** 我們在寫這份文件之前的一份舊紀錄裡，
同一個內容的存檔 id 是完全不同的一組，**但 sha256 一模一樣**。
容器重啟會產生新的 id，位元組不變則雜湊不變。這是一個**你可以獨立驗證的宣稱**。

### 我們對 grader 宣稱的是

> 模型看完頁面之後，會提出一個答案。**那個提議被當成假設，不是結論。**
>
> 接著，決定性的程式碼會做這件事：**回到當時存下來的那份頁面位元組裡，重新去找那個值一次**，
> 而且必須是**透過任務指名的那個標籤**找到的。找不到，這次執行就不算成功——不管模型多有把握。
>
> 一句話：**模型提議，決定性程式碼裁決；而裁決的對象是保存下來的位元組，不是即時的頁面。**
>
> **為什麼一定要對「保存下來的位元組」而不是「即時頁面」。**
> 如果回頭去打即時頁面來驗證，你會得到另一種失敗：**頁面在這中間變了，檢查通過了，
> 而這兩件事毫無關係。** 你以為你驗證了答案，其實你只是驗證了「現在的頁面上有這個字串」。
>
> 對存下來的位元組驗證，換到的是三個性質：
>
> 1. **第三方可以重跑。** 存檔以內容雜湊定址，而且公開可取。任何人都可以拿同一份位元組，
>    自己重跑一次驗證。
> 2. **不受頁面漂移影響。** 執行之後頁面怎麼變，都不可能把一個沒過的檢查變成過的。
> 3. **不可能靠巧合滿足。** 值必須從任務指名的**標籤**結構性地連過去。
>    「頁面上某處剛好出現這個字串」不算答案——那是子字串搜尋，不是「這個商品的 UPC 是多少」。
>
> **我們也對「不存在」設了同樣高的標準。** 「我找了，沒找到」**不構成**「沒有東西符合」。
> 要回報「查無此物」，系統必須拿出正面的證明：要嘛真的定位到頁面上的「無結果」元素，
> 要嘛證明整個結果集都被讀過了。做不到就棄權。
>
> **弱點。** **驗證器檢查的是「這個宣稱和我們保存的證據一致」，不是「這個宣稱在世界上為真」。**
> 這兩件事不一樣，頁面上寫得很清楚。
>
> 在我們自己的評測裡，四項承諾中**只有一項**有辦法**獨立推導出正確答案**再去比對——
> 就是表格排序那一項：我們的評測工具自己去抓那篇條目、自己判斷該用數值還是字典序
> （這個判斷本身就是陷阱：同一欄用兩種排法會得到不同的第一列，而且兩種看起來都很合理）、
> 自己排一次、比對第一列。**另外三項沒有獨立的正確答案來源**，特別是展開摺疊區塊那一項——
> 那個狀態只有在互動發生之後才存在，任何直接抓取都會跟一次正確的執行對不上。
>
> 所以我們公布「沒有出現過『通過驗證但其實是錯的』」這句話的時候，同時公布它的邊界：
> **在那三項上，這個保證靠的是我們自己的檢查器，而那正是被檢查的東西。**
>
> 我們已經試過什麼：我們一度在評測題目上寫著每一題都有獨立的正確答案來源——
> **那是假的，程式從來沒有實作過。** 這件事是我們自己審查時抓到的，而且它是我們找到的所有缺陷裡，
> **第一個方向是「讓沒被證明的東西看起來被證明了」的**。修法是真的把表格排序那一項的獨立推導做出來，
> 然後把其他每一題的欄位改寫成「這一題實際上被什麼檢查」，三選一：獨立推導、證據重查、軌跡檢視。

### 請你查證

**這是全文最重要的查證清單，請一項一項做。**

1. **詳情頁上真的顯示了雜湊嗎？** 確認 Claims 面板的 Artifact 那一行有 `sha256 <十六進位>…` 字樣，
   而不是只有一個 id。
2. **打開那份存檔，確認宣稱的值真的在位元組裡。**
   搜尋 `No products match that search` 和 `0 results for`。**兩個都要找到。**
   如果只找得到一個，記成不相符並說明是哪一個。
3. **確認頁面顯示的雜湊前綴，和整份位元組是同一個東西。**
   如果你的工具能算 sha256，請算一次並和頁面上的前綴比對。
   如果不能算，就記錄「頁面顯示的前綴是 X，存檔長度是 Y bytes」，並記成**無法判斷雜湊本身**——
   這是誠實的回報，不要假裝驗過。
4. **確認 `items` 這個宣稱是「沒通過」而不是被藏起來。**
   我們的說法是「一個失敗的宣稱要留在頁面上，帶著它的原因」。
   請確認它顯示為紅色的 `not verified` 加 `locator_not_found`，而且有一段說明。
   **一個把失敗宣稱隱藏起來的介面，會讓這整套宣稱失去意義。**
5. **確認「verified 不等於真」這句話真的印在頁面上。**
   Claims 面板最上面有一個藍框說明區。請確認它裡面有沒有明講
   「這不代表這個宣稱在世界上為真」。**如果沒有，這是一個嚴重的不相符。**
6. **What was checked 面板：確認每一道閘門都列出來、有 pass/fail、可以展開看細節。**
   我們看到的閘門名稱包括：`postcondition_frozen`、`artifact_available`、
   `artifact_origin_is_the_named_site`、`artifact_source_is_accounted_for_by_the_trace`、
   `landing_explained_from_the_plan_target`、`required_actions_present`，
   再加上每一個宣稱各一道。**請確認至少有一道是關於「條件在事前被凍結」的。**
7. **確認「N of M claims were independently re-resolved」這句話有出現，而且 N 和 M 對得上你在
   Claims 面板數到的數量。** 這是一個很容易寫錯又很難被發現的地方。
8. **加碼（免費）：去找一筆在真實網站上、模型驅動的成功。**
   在 Runs 表格找 `On the Wikipedia list of S&P 500 companies, sort the constituents table by
   GICS Sector descending…` 那一列（如果還在）。
   它的存檔很大（我們看到的是 **1,937,772 bytes**）。請注意一個陷阱：
   **標籤文字 `GICS Sector` 在原始 HTML 裡是被標籤切開的**
   （`<a ...>GICS</a> Sector`），所以你直接搜尋 `GICS Sector` 會**找不到**，
   但答案的值（我們看到的是 `AES Corporation`、`Independent Power Producers & Energy Traders`）
   **確實在裡面**。
   如果你因為搜不到標籤就判定「驗證是假的」，那就是被這個陷阱騙了。
   **請把這件事寫進你的報告**——因為評分者也會踩到，而我們的文件沒有事先警告他。

---

## 步驟 6：單筆執行詳情頁上的每一塊 `【免費】`

### 怎麼操作

挑一筆**內容最豐富**的執行（承諾層、模型驅動、成功、步數 10 以上），從上到下讀完整頁。
把每一個 `detail` 收合區塊至少展開一個。

### 應該看到什麼

我們數到的是**九個面板**，由上而下：

| 面板 | 你可以從這裡確認什麼 |
|---|---|
| **標題與徽章列** | 任務原文、分層、最終狀態、失敗原因、`does not count as success`。如果這是一個「不乾淨的安靜結果」，這裡會多一個紅色徽章 |
| **Claims and evidence** | 每一個回報的值：綁到哪個標籤、從哪一段抽出來、走的結構路徑、來自哪一份存檔、那份存檔的雜湊、擷取日期、可以點開的原始位元組連結 |
| **What was checked** | 驗證器跑過的每一道閘門，依序列出，各自 pass / fail，可展開細節。**執行器不能自己設定成功狀態** |
| **Frozen postcondition** | 瀏覽器打開之前凍結的那組條件，以及它的雜湊。若這筆執行在選定計畫之前就停了，會明說「沒有凍結任何條件」 |
| **Action trace** | 每一步做了什麼、幾毫秒、有沒有診斷出原因、有沒有換策略、有沒有抓存檔。上方標明「幾次重試、幾次真正的策略切換」 |
| **Evidence artifacts** | 所有存檔的清單：種類、來源網址、擷取日期、位元組長度、雜湊、狀態（stored / pinned / expired） |
| **Timing** | 佇列等待、執行秒數、到第一個結果的時間、等模型的秒數、每步耗時的中位數 / p90 / 最大、最慢的是哪一步 |
| **Budget and provenance** | 用掉幾步（幾分之 25）、幾次模型呼叫（探索 / 修復分開）、token 進出、花了多少錢、走模型路徑還是決定性路徑、金鑰層級、瀏覽器世代、執行 id |
| **Locator memory** | 這個版本有沒有記憶體、記憶體的邊界是什麼 |

### 我們對 grader 宣稱的是

> 給評分者的那份文件，把這一頁列成**七塊**（把 Timing 和 Budget 併成一塊，
> 而且**沒有提到 Locator memory 那一塊**）。
>
> 這一頁有一個我們特別想請你看的東西：**「不乾淨的安靜結果」徽章。**
>
> 一個棄權和一個正確的棄權，長得一模一樣。問題是：如果是**我們自己**在把頁面壓縮送給模型的時候，
> 把那個元素丟掉了，那模型誠實地說「我看不到」，就會由一個其實有答案的頁面產生。
>
> 所以每一個安靜的結果（棄權、查無此物）都會被拿去跟「我們自己刪掉了什麼」對照一次，
> 對得上就掛徽章。
>
> 弱點：**那個稽核只涵蓋我們想到要去看的東西。** 一個由我們沒有計數器的原因造成的棄權，
> 跟一個正確的棄權，在我們的儀器上是分不出來的。這一條寫在頁面上，也寫在報告裡。
>
> 另外，關於自我維護的記憶體，我們宣稱：它**只從「驗證通過」的執行學習**，
> 它記的是元素的身分，**從來不記值**，所以記憶體裡的東西不可能變成一個答案。
> 而且一個被記住的定位器，會像其他任何定位器一樣被重新解析、重新驗證——
> **記憶體省的是搜尋的力氣，永遠不是證明。** 連續失敗三次會被隔離。

### 請你查證

1. **數一下這一頁有幾個面板。** 我們給評分者的說明列了七塊，實際上有九塊，
   而且**沒有提到 Locator memory**。請確認這一點，並判斷「漏列」是不是一個值得修的問題。
2. **Timing 那一塊有一個叫 `Reportable` 的欄位，只在「不可引用」時才會出現。**
   請找一筆有它的執行（走開發快取的執行會有）。如果所有執行都沒有這一欄，
   那代表這些數字都是可引用的——這是好事，但請確認你有看到這個欄位的機制存在，
   不要只因為看不到就記成缺失。
3. **「不乾淨的安靜結果」徽章：請主動去找。**
   在 Runs 表格裡找所有 `unsupported`、`unverified`、`no_result_verified` 的執行，
   逐一打開，看有沒有任何一筆掛了紅色的「not a clean …」徽章。
   給評分者的說明現在直接指名了一筆（MDN `Array/flat` 那題）。
   請確認那一筆真的掛著徽章，**並且**自己掃過其他安靜結果，
   判斷這個機制是只在那一筆上成立，還是普遍成立。
   如果你一筆都找不到，那代表這個機制在你這次的資料上沒有被觸發過，
   請記成**無法判斷**，而不是記成「有」或「沒有」。
4. **Locator memory 面板：確認它說的和健康頁的計數器一致。**
   到 <https://wf-agent.zeabur.app/healthz>，找 `locator_memory` 那一塊。
   我們查證時看到的是：`rows_stored` 是個位數，而 `hits`／`uses`／`heals`／`quarantined` 全部為 0（存了幾筆會隨部署變動，請以你當下看到的為準）。
   **也就是說：記憶體裡有五筆，但從來沒有被用過一次。**
   請確認這個數字，並判斷：詳情頁上那段講記憶體的說明，會不會讓一個評分者以為它正在發揮作用？
5. **Action trace 裡的每一步，有 `artifact` 連結的請點一個進去，確認它真的回傳位元組**
   （而不是 404 或一段 JSON 錯誤）。
6. **Evidence artifacts 表格：有沒有任何一列是 `expired`？**
   如果有，確認它**保留了 id、雜湊、長度、來源網址、擷取日期**，而不是變成一個死連結。
   我們宣稱「存檔過期是一個有日期的狀態，不是一個指向空無的指標」。

---

## 步驟 7：讀懂七種狀態 `【免費】`

### 怎麼操作

回到首頁的 Runs 表格。**把 Outcome 欄和 Counts as success 欄逐列對照一遍。**

### 應該看到什麼

`succeeded_verified` 和 `no_result_verified` 那兩列的 Counts as success 是綠色的 `yes`，
其他全部是 `no`。

### 我們對 grader 宣稱的是

> **七種最終狀態，這是完整的一份，不會有第八種。**（表格見本文開頭。）
>
> **一條規則請直接記住：`partial` 和 `unverified` 永遠不會被當成成功。**
> 它們看起來像「差不多有做到」，但在這個系統的定義裡，它們跟 `failed` 一樣不進成功率、
> 不會被聚合、不會在任何地方被描述成成功。
>
> 頁面上每個地方都有一個獨立的 `Counts as success` 欄位。
> **請看那一欄，不要從狀態字面自己推斷。**
>
> **這件事為什麼做得到。** 因為狀態和失敗原因都是**封閉集合**——七種狀態、十八種失敗原因，
> 要新增只能透過一次書面修訂。**一個「絕不把部分結果講成成功」的保證，
> 只有在集合封閉、規則是機械式的時候才有意義**；如果狀態可以隨手新增，那句保證就只是一句話。

### 請你查證

1. **首頁表格裡，有沒有任何一列的 Counts as success 和上面那張對照表衝突？**
   （例如一個 `unverified` 卻標成 yes。）**這是最容易造假、也最容易查的一項，請仔細做。**
2. **有沒有出現過第八種狀態？** 把你在整站看到的所有狀態字串收集起來，
   確認它們全部落在那七個裡面。
3. **失敗原因是不是十八個？** 到 <https://wf-agent.zeabur.app/coverage>，
   第二張表（標題 `failure_class`）數一下有幾列。我們宣稱是 18。
4. **首頁最下方的頁尾有一句總結。** 我們看到的是：
   「A result is `verified` only when deterministic code re-resolves a stored anchor inside the
   full preserved artifact. `partial` and `unverified` are never counted as success.」
   請確認它還在，而且和上面那張表一致。

---

## 步驟 8：去看失敗——這頁可能是最值得看的 `【免費，除非你決定送出】`

### 怎麼操作

1. 導航到 <https://wf-agent.zeabur.app/support>。
2. 捲到標題是 **Known limitations you can reproduce** 的面板。
3. **不要送出這些任務**——它們大多都已經在首頁的 Runs 表格裡有既有紀錄了。
   請改成：把限制表上的每一條「任務文字」拿去和首頁 Runs 表格比對，
   **看實際跑出來的結果和限制表公告的結果一不一樣**。

### 應該看到什麼

限制表有七列，每一列是「一個你可以直接貼進輸入框的任務」＋「會產生什麼結果」＋「為什麼」。

**這一步不給你答案。** 上一輪審查者指出，我們把「公告 vs 我們實際看到」的對照表先寫好，
等於答案先發下來，逐條比對的獨立性被削掉一半。所以現在只給你做法：

把限制表上的七條任務文字，逐一拿去和首頁 Runs 表格上同名的那一列比對
（`terminal_status` 與 `failure_class` 兩個都要比），列出你自己的對照表。
**其中至少有一條的公告 `failure_class` 與實際不符**——請自己找出是哪一條，
並判斷這對一份「宣稱自己可執行、可被推翻」的清單意味著什麼。

### 我們對 grader 宣稱的是

> **這份清單裡的每一條，都是對線上系統實際執行過、把觀察到的結果記下來的**，不是憑印象寫的。
> 我們另外寫了一支程式，會把整份清單（包含清單自己聲稱的「這樣改寫就會成功」的講法）
> 拿去對部署跑一遍。
>
> **第一次跑那支程式的時候，七條裡有四條沒辦法照著寫的方式重現。**
>
> - 一條聲稱「把條目名稱補上就會成功」——**它從來沒成功過**，三十秒就能重現。
> - 一條寫的失敗原因分類是錯的，實際跑出來的那個更精確。
> - 兩條是**系統已經變好了，而文件爛在原地**：其中一個任務已經開始成功，
>   另一個已經真的能證明「不存在」了。
>
> **發表一個已經不會發生的棄權，跟發表一個從來沒 work 過的補救講法，是同一種缺陷。**
> 這也是為什麼我們堅持限制清單必須是可執行的：它可以被推翻，所以有人會發現。
>
> 順便，那次執行還抓到一個沒有任何測試抓到的回歸：一種頁面擷取動作佔用了自己的一格步數預算，
> 導致擷取比較多的執行只剩下一半的可用步數。
>
> **一個實用提醒（你明天很可能會遇到）：** 如果你在 books.toscrape.com 上看到
> `blocked / robots_disallowed`，**請先打開軌跡再下結論**：
>
> - `source: "matched"`（旁邊附上命中的 `rule` / `pattern` 原文）→ 站方真的禁止
> - `source: "unfetchable"` / `unparseable` → **我們**讀不到那份規則，所以保守拒絕
>
> books.toscrape.com **根本不發布**機器人規則檔（回 404，我們讀成不受限），所以在那個站
> 看到的幾乎一定是後者，也就是當下的網路問題，重跑通常就好。
> 這不是假設。這件事在我們那組完全沒看過的題目上發生了三次，而且我們一開始把它讀錯了。

### 請你查證

1. **列出你自己的七條對照表。** 有任何一條的實際結果和公告不同，就是**不相符**，
   而且是嚴重的——因為這份清單的整個賣點就是「它是可執行的、可以被推翻的」。
   請一併確認 `source` 的實際欄位值與文件寫的一致。
2. **第 4 條那筆的軌跡：確認它引出了規則原文。**
   打開那筆 `blocked` / `robots_disallowed` 的執行，展開 `policy_check` 那一步的 detail。
   我們查證時看到的說明是：
   「`en.wikipedia.org/robots.txt` disallows this path. Matched rule: `Disallow: /wiki/Special:`
   (group `User-agent: *`).」
   請確認 detail 裡有一個 `source` 或等效欄位，值是 `rule`（不是 `unfetchable`）。
   **這是我們區分那兩種意思的唯一憑據，如果頁面上根本沒有這個欄位，我們給評分者的建議就是空的。**
3. **限制表上有沒有明說「這份清單是可執行的」，以及怎麼執行？**
   確認那段文字有出現，並判斷它對一個不會跑指令的評分者有沒有用。
4. **這一節我們沒有對評分者說的事，請你判斷要不要補：**
   限制清單的可執行檢查**尚未對最終版重跑**（這是我們「已知還沒修」清單的第 3 項）。
   所以上面那張表是「上一次跑的結果」而不是「這個版本的結果」。
   你剛剛做的逐條比對，**實際上就是那次沒跑的檢查**。請把你的結果寫清楚。
5. **建議：不要為了這一節送出任何任務。** 上面七條在首頁 Runs 表格裡都已經有既有紀錄。
   如果有某一條找不到對應紀錄，記成**無法判斷**比花錢重跑更有價值——
   因為「限制清單上的東西在首頁上找不到」本身就是一個發現。

---

## 步驟 9：涵蓋頁與健康頁 `【免費】`

### 怎麼操作

1. 導航到 <https://wf-agent.zeabur.app/coverage>。讀兩張表。
2. 導航到 <https://wf-agent.zeabur.app/healthz>。這是一份 JSON。

### 應該看到什麼

**涵蓋頁**有兩張表（`terminal_status` 七列、`failure_class` 十八列），欄位是
Value / Due at / Produced / Observed from / First run / Times。
`Produced` 欄有四種徽章：`yes`（綠）、`overdue`（紅）、`not due yet`（灰）、`not built`（黃）。
`Observed from` 欄會分「a run of the product」和「regression suite」——後者代表這條路徑
是被測試走過的，不是被真實執行走過的。

我們查證時看到的：

- **七種終局狀態全部是 `yes`**，全部由真實執行產生（`partial` 只有 1 次、`locator_not_found` 只有 1 次）。
- 十八種失敗原因裡，有 **10 種在這次開機後從未被產生**：
  `queue_full`、`session_quota`、`timeout`、`site_unavailable`、`internal_error`、
  `provider_quota`、`provider_error`、`token_budget_exhausted`、`context_budget_exceeded`、
  `output_truncated`。頁面上這些會標成紅色的 `overdue`，頁首的總結徽章因此是**沒有通過**的
  （它把這十項列出來）。**這是誠實的讀數，不是缺陷**：這些原因在這個部署上真的一次都沒發生過，
  而這一頁的工作就是說出來。其中 `queue_full` 與 `robots_disallowed` 你可以自己觸發。
- **`injection_detected` 標的是黃色的 `not built`**，`Due at` 欄是一個破折號，
  值旁邊直接附上理由（注入偵測器沒有做、安全題組是被砍不是被延，這個值之所以還在，
  是因為分類集合是封閉的）。**它不帶任何未來時程，也不會被算進逾期清單。**

**健康頁**是一份 JSON，我們查證時的重點數字：

| 欄位 | 值 |
|---|---|
| `git_sha` | 一組 commit 代號。**請記下你看到的那個**——它才是你這次評審的對象，我們文件裡引用的任何一組都可能比它舊 |
| `queue` | concurrency 2、depth 2 |
| `browser` | connected true、rss 約 696 MiB、回收政策 50 次 / 6 小時 / 1400 MiB |
| `storage` | 220 份存檔、61.1 MiB / 4000 MiB、保留 14 天、**掛在持久化磁碟上並且真的寫過一次探測檔** |
| `locator_memory` | rows_stored 為個位數，hits／uses／heals／quarantined 全部為 0。**「有存、但一次都沒被查詢」正是一個健康部署該有的樣子**——記憶體只在某個定位方式失效時才會被叫出來，所以頁面沒改版時這些數字就不該動。這一頁會把這個說明寫在計數器旁邊（`reading` 欄），因為一個裸露的 0 讀起來像「這機制做了但是死的」 |
| `budgets` | 180 秒 / 25 步 / 12 次模型呼叫（8 探索 + 4 保留給修復） |
| `provider_spend` | **`cumulative_billed_usd: 0`**、`cumulative_notional_usd: 0.066`（若站台已切到下面那個付費政策，這個實付數字就可能不再是 0） |
| `credentials` | 憑證政策這一欄**你看到的可能是兩種之一**：計分期間用的是**只用免費額度**的政策（實付一律 0，額度用完就誠實回 `blocked / provider_quota`，不會偷偷升級）；計分結束後公開站可以切成**免費額度優先、被限流或用完時自動 fallback 到付費金鑰**的政策，並帶自己的累計上限。**請以你當下看到的那一欄為準**——這一頁會直接說出現在生效的是哪一個、以及哪些層級在該政策下可用。兩種政策下都**不會顯示金鑰內容、前綴或長度** |
| `planner` | available true，並註明「啟動時真的打了一次最小呼叫」 |

### 我們對 grader 宣稱的是

> **`/coverage`** — 這個部署實際上產生過哪些結果狀態。它的用途是讓你分辨
> 「這個狀態我們宣告了但從來沒有任何程式路徑到得了」跟「這個狀態真的發生過」。
> 舉例來說，注入偵測是一個我們宣告了、但**目前沒有任何程式路徑會到達**的狀態，
> 這頁把它標成**「未建」並附上理由**，`Due at` 是一個破折號：它不帶任何未來時程，
> 也永遠不會被算成逾期。
> 注意它只計算「上次重啟以來」，所以剛部署完那份清單會很長，那不是壞掉——
> 頁首的總結徽章多半是「沒有通過」、後面掛著十種從未被產生過的失敗原因，
> **那是誠實的讀數，不是缺陷**。
>
> **`/healthz`** — 運行狀態的 JSON：目前跑幾筆、佇列多深、瀏覽器記憶體、存檔用了多少空間、
> 離上限多遠、以及自我維護記憶體的計數器。
>
> 兩個容易讀反的欄位：自我維護記憶體的計數器多半是「有存、但一次都沒被查詢」，
> **那正是健康部署的樣子**（記憶體只在定位方式失效時才介入），這一頁會把這句話寫在計數器旁邊；
> 憑證政策那一欄可能是「只用免費額度」或「免費優先、必要時 fallback 到付費」兩者之一，
> **請以你當下看到的那一欄為準**。

### 請你查證

1. **涵蓋頁對 `injection_detected` 到底說了什麼？**
   我們給評分者的說法是「這頁會直接說它沒有任何程式路徑會到達」。
   **這一條原本是不相符的**：那一列曾經是灰色的 `not due yet` 加一個未來的版本代號，
   等於替一件被砍掉的工作編了一張時程表。現在應該是黃色的 `not built`、`Due at` 是破折號、
   旁邊附著理由。請獨立確認**那一列沒有任何未來時程**，也沒有被算進頁首的逾期清單，
   並判斷附上的那句理由對一個沒有內部背景的讀者夠不夠清楚。
2. **涵蓋頁最上面的版本代號，和首頁橫幅上的版本代號，是同一個嗎？**
   **這一條原本也是不相符的**：涵蓋頁自己寫死了第二份副本，而那份副本已經落後一階。
   現在兩邊都從同一個來源讀。請確認兩個代號一致，若不一致請原樣記下兩個值。
3. **`Observed from` 欄有沒有任何一列標成 `regression suite`？**
   我們的說法是「『測試到得了』和『產品真的做過』是兩種不同的宣稱，所以要分開標，不能混在一起」。
   請確認這個區分真的在頁面上生效。
4. **花費的數字對得上嗎？** 我們對評分者公布的數字是「累計實付 USD 0.1515」，
   而健康頁上這個公開展示行程的 `cumulative_billed_usd` 不會等於它。
   **兩個不衝突，但頁面上沒有把話講得夠白**：那個 0.1515 來自另一條持有付費金鑰、
   負責計分的工作流程，兩個行程讀不到彼此的帳本。
   公開展示這一側則要看它現在跑在哪個憑證政策上：**只用免費額度**時它的實付一定是 **0**；
   **免費優先、必要時 fallback 到付費**時，它會有自己的一個小額累計數字和自己的上限。
   **請先記下你看到的政策與實付數字，再判斷這一頁有沒有把「為什麼這裡的數字和報告裡的不一樣」
   講清楚**（我們看到 `provider_spend.note` 和 `meaning` 兩個欄位有寫，但是英文、埋得很深）。
   然後判斷：**一個評分者對照這兩個數字，會不會以為我們在灌水？**
5. **存檔的持久化探測。** 確認 `storage.write_probe` 裡有 `mounted: true`、`writable: true`、
   以及一個時間戳。我們的說法是「只檢查路徑存不存在的話，未掛載、唯讀、磁碟滿這三種狀況會一律通過，
   而且要等到有人去點證據時才會被發現」。
6. **`unhealthy_because` 是空陣列嗎？** 如果不是，把裡面的內容原樣記下來。

---
---

# 兩節總結（與給評分者的版本一致，供你對照）

下面這兩節是我們**逐字**給評分者的自評。**你的工作是判斷它們準不準、公不公平、
有沒有哪一條在你剛才的實地操作裡被推翻。**

---

## 做得好的地方，以及我們具體怎麼做到的

**一、證據優先的驗證。** 上面步驟 5 講的那件事，是整個系統的地基：
條件在瀏覽器打開之前凍結並雜湊，模型只提議，決定性程式碼回到保存的位元組裡重新解析，
而且必須經過任務指名的標籤。這讓「驗證」變成一件第三方可以自己重跑的事，
而不是一句我們自己說了算的話。

**二、承諾寫成「網站 × 操作」，並且附上量到的數字。**
宣稱「我們能處理任何網站」很容易，而且無法被否證。
**宣稱四項具體操作、在我們不控制的網站上、附上量測數字和信賴區間，是同一句話的可否證版本。**
代價是我們公布的成功率覆蓋的表面，比一個講得更含糊的產品小很多——
而且在我們把自己寫的測試站那三項撤下來之後，它又變得更小了。

**三、失敗帶著具體的原因，而不是一個籠統的錯誤。**
七種狀態 × 十八種原因，都是封閉集合。一個失敗的執行會告訴你它停在哪一步、哪一頁、
哪一部分條件沒滿足。**「這一步丟了例外」不是一個診斷。**

**四、「不存在」必須要有正面證明。** 這條讓我們在評測上實實在在丟掉分數
（多頁分類的問題會棄權而不是回答），我們留著它。

**五、重試和真正的策略切換被分得很開。** 同一個策略再跑一次叫**重試**，會被記錄成重試——
**它不是自我修正**，把它算成自我修正是造假這項要求最容易的方法。
真正的**策略切換**是換到另一個策略家族（無障礙樹 → 文字錨定 → 結構 → 替代路線 → 替代表徵），
而且必須由一個**具名的、被診斷出來的原因**觸發，原因來自一個封閉集合。
詳情頁上兩個數字是分開列的。

**六、自我維護的記憶體，只從「驗證通過」的執行學習。**
它記的是元素的身分，**從來不記值**，所以記憶體裡的東西不可能變成一個答案。
而且一個被記住的定位器，會像其他任何定位器一樣被重新解析、重新驗證——
**記憶體省的是搜尋的力氣，永遠不是證明。** 連續失敗三次會被隔離，超過確認期會失效，
計數器公開在健康頁上。

**七、拒絕用螢幕座標點擊。** 這是最後一種手段，我們在規劃階段就把它擋掉了。
盲目點座標會產生的，正是這個產品存在要防的那種失敗。

**八、頁面上的文字一律當成不可信資料。** 網頁上的字永遠不能改變目標、分層、政策、預算或記憶體。
這是**結構上**做到的（記憶體只餵給定位器解析，從來不會被當成自由格式的指令注入給規劃器），
而不是靠拜託模型。

---

## 還有缺陷的地方，為什麼還有，以及我們已經嘗試過什麼

這一節是刻意寫長的。

### 一、完全沒看過的題目，結果比我們自己的題目差很多

| 題組 | 誰出的 | 結果 |
|---|---|---|
| 開發用（自建） | 我們 | 計入通過率的 11 題中通過 **10 題** |
| 實驗用（從沒碰過的網站） | 我們 | 通過驗證 **3/10**，95% 信賴區間 **0.11–0.60** |
| **保留題組（開發過程從未看過）** | 產品負責人，工程端全程沒看過 | **1/8**，95% 區間 **0.02–0.47**；承諾層 4 題中 **1 題**，區間 0.05–0.70 |

**這個落差本身就是結論，不是註腳。** 只跑一次，跑壞了也不重跑——第一次跑出來的就是分數。

**那八題裡有五題從來沒有開始瀏覽。** 而這五題要拆成兩半，因為它們的性質完全不同：

- **三題是一次機器人規則檔取得逾時。** 同一個網站、同一個原因：我們讀不到那份規則，所以保守拒絕。
  **那不是政策覆蓋面的發現，是一次可用性事件**——那個網站根本不發布規則檔，
  而且在那之前二十分鐘的另一輪計分裡，同一個網站的七題全部正常瀏覽。
- **兩題是任務沒有指名頁面。** 這個是真的，就是步驟 2 講的那條刻意的限制。

所以那 1/8 是一個**下限**，而且它量到的「可用性」成分不亞於「能力」成分。
**但它仍然是分數**，我們不重跑。

### 二、我們一開始把上面那三題讀錯了，而且錯得很典型

**我們最初的結論是「這三題證明我們對『一般任務』的直覺比我們自己的政策寬」。那是錯的。**

那個結論是**看著失敗原因的統計圖表下的判斷，在打開任何一筆執行紀錄之前**。
它之所以能活下來，是因為那一輪的摘要裡沒有任何東西可以反駁它——
摘要的精確度取決於失敗原因分類的精確度，而那個分類不夠精確：
**同一個標籤同時承載「站方禁止我們」和「我們讀不到站方的規則」兩件事。**
兩種拒絕都是對的，但只有一個標籤是對的。

**打破它的是點了一個連結。** 打開那三筆的執行紀錄，看到的不是規則，是一行逾時錯誤。

我們把這件事完整寫進報告裡，指名道姓，因為：這份提交裡其他所有的缺陷，
都是「一個檢查回報了一個巧合」。**這一筆是我們自己，在最後一天，做了大型語言模型會做的事：
產出一個貌似合理、而摘要無法否證的結論。** 抓到它的機制，
是我們一小時前才手動搶救出來、剛發佈上線的那批證據。

**為什麼還沒修：** 把那個標籤拆成兩個，會改變一個已經被計分結果報導過的分類。
在死線前一天做這件事，換來的風險大於收益。我們選擇留著缺陷、寫清楚它，
並在支援頁和上面步驟 8 教你怎麼分辨。

### 三、分層路由認得的句型比承諾窄

見步驟 3。承諾層四題有兩題沒走到承諾的表面。**我們自己的題目量不到這件事**，
因為那些題目是知道系統吃什麼句型的人寫的。同一族的上游版本我們在凍結前修掉了一個
（商品頁被寫死成單一本書），但路由層這個更上游的版本，是保留題組替我們找出來的，
來不及再修一輪。

### 四、四項承諾裡，有三項沒有獨立的正確答案來源

見步驟 5。只有表格排序那一項，我們的評測工具可以自己推導出正確答案再比對——
而且在最後一輪計分裡它真的跑了，八個儲存格全部一致。
**另外三項的正確性，靠的是我們自己的驗證器**，而那正是被檢查的東西。
這一條我們選擇公開承認，而不是讓那句「沒有出現過通過驗證但其實是錯的」不加限定地站在那裡。

**一個無法登記某件事發生的儀器，量出來的零不是零，是沒有資訊。**

### 五、多頁的涵蓋範圍證明不了，所以那些問題會棄權

跨越多頁的分類會跨越多份存檔，而這個版本對一份存檔驗證。單頁的分類會被證明並回答，
多頁的會被回報成「未證明」而不是拿我們剛好看到的那一頁去回答。
這是「不存在必須有正面證明」那條規則直接的代價，我們留著它。

### 六、步數上限會讓長翻頁任務沒有答案

見步驟 4。fail-closed 是刻意的，另一個方向是把「剛好翻到的那一頁」當成最後一頁回報。

### 七、安全測試套件和第二個題目沒有做，這是刻意砍的

- **安全套件**：現在存在的是出口防護、機器人規則執行、拒絕分類法，全部都是承重的、有測試的。
  **不存在的**是安全測試題組和注入偵測器。
- **第二個題目（SEC 10-K 抽取）沒有做。** 作業要求至少完成一項。我們留下的是一份完整、
  凍結的介面契約——解析規則、修訂順序、上限行為、雜湊——**而契約後面什麼都沒有**。
  在只剩兩天的時候，替一個不會存在的產品寫一份完整規格，是從那個會存在的產品身上直接減去。
  我們把它標成「設計完成、刻意未實作」，因為那份設計是真的工作，假裝不是才是反效果。

### 八、我們在自己的檢查機制裡找到二十個缺陷，修好十七個

一個核心宣稱是「我們的檢查是真的」的系統，必須預期那些檢查本身會出錯。
其中十個**全部是同一個物種：一個回報了巧合的檢查**；兩個是它的反面——
不是一個檢查報告了不實的事，而是**根本沒有檢查**；最後八個是第三種——
**我們用自己的話描述自己的頁面，而沒有任何東西在檢查那些話是否還成立**。
第三族最大，也最晚才浮現：讀 repository 找不到其中任何一個，只有實際操作部署才會撞到。

前五個是開發中找到的，例如：一個從來沒被評估過卻被記錄成「已滿足」的約束；
一個由別的程序的副作用滿足的前置條件；一個在沒有東西可檢查時回報「通過」的空分支；
一個把缺陷編碼成需求的測試（所以修掉缺陷反而把測試弄壞了）；
以及那個**宣告了但從來沒實作的獨立答案來源**。

**前四個讓正確的行為看起來壞掉，第五個讓沒被證明的行為看起來被證明了。**
那個不對稱，就是為什麼第五個活得最久。

後五個裡最值得看的是這個：有一道硬性閘門，它的成敗**取決於一個變數是在哪一瞬間被讀的**。
同一組輸入，前一輪計分通過、下一輪失敗。更值得看的是，**第一次的修法是錯的，而且錯在前提**：
我們假設某個維基百科條目有 HTTP 重導向，實際用瀏覽器量過之後發現它回 200、完全沒有重導向，
是網站自己的腳本在大約兩秒後改掉網址列的。照第一次的修法，
會把一個間歇性的失敗變成一個穩定的失敗。

**還有三個沒修**，理由都一樣：它們是在建置凍結之後才發現的，
而修它們會讓部署的版本在「計分結果有報導過的行為」上和計分的版本不一致。
**我們選擇讓程式碼難看，讓兩輪計分描述同一個版本。**

**第十一個是反過來的，而且它就發生在你要點的那一頁上。**
前十個都是「一個檢查觸發了、然後報告了不實的東西」。這一個是**根本沒有檢查**：
執行詳情頁——也就是軌跡、證據出處、拒絕理由所在的那一頁，這份文件叫你點的那一頁——
連續三天回 500，橫跨全部三輪計分。原因是加入自我維護記憶體的那次修改，
在頁面上加了一段要讀「這個版本有沒有記憶體」的模板，卻沒把那個資訊傳給那一頁；
模板引擎對缺漏的變數是沉默的，只在被要求取屬性時才爆，所以其他頁面完全正常，測試也全綠——
因為**從來沒有任何測試打開過這一頁**。找到它的方式，是送審當天有人做了評審會第一個做的事：
點開一筆執行。已修，並補上「每一種結束狀態各一筆、都要能打開」的測試。

**第十二個是第十一個的另一半，隔天才發現。** 為第十一個補的測試證明那一頁**打得開**，
卻沒有看著它**動**——而動起來的那部分有同一個洞：進度是靠一條即時串流推的，
串流斷掉時沒有任何後備。連線一斷（我們自己換容器時就會有十幾二十秒的斷線），
頁面就停在最後一行進度上繼續轉，而那筆執行其實**早在 13 秒前就成功了**。
**我們自己的介面，對我們自己的執行狀態，給了一個看起來合理而錯誤的說法**——
正是這整份提交在反對的那種失敗，只是發生在我們自己身上。已修：串流斷掉時會**明講**
「即時更新已關閉，改為每三秒輪詢」，滿五分鐘就停下來請你重新整理，而不是無限轉。
**無聲地降級只是把同一個缺陷換一件安靜的外衣**——你必須能分辨自己看的是哪一種。

**還有三個，是最後一次審查在網站上跑出來的，而且三個都是我們用來描述自己的句子。**
覆蓋頁——那一頁的工作就是說明哪些結果從來沒被產生過——把一個**被砍掉**的能力
顯示成「排程中、還沒輪到」，也就是替一件不會發生的工作編了一張時程表；
而它旁邊的文件宣稱那一頁講清楚了，其實沒有。同一頁還把「目前做到哪個階段」
寫死成第二份副本，而那份副本已經落後真正的版本一階。首頁則用「最前面幾筆」
去指認預先執行的示範，但那張清單是新的在前，所以任何人一送出任務，示範就會往下沉——
審查者照著那句話看最上面，看到的是四筆普通執行。三個都已修，改法一樣：
讓頁面去講它能從程式碼推導出來的東西——被砍的能力給理由不給時程、
階段從版本自己讀、示範用標記指認而不是用位置。
**沒有一個影響任何結果；三個全部是我們對自己的描述過期了，而且都朝著對我們有利的方向過期。**

### 九、第一輪獨立審查，找到十一個，其中八個是同一族

**（這一節是給評分者的說明裡新增的，內容就是上一輪你的前任找到的東西。
請確認我們在這裡的自述，與 `acceptance-report.md` 的實際內容一致，沒有淡化。）**

> 我們在送審前請了一個獨立的審查者，只用瀏覽器、不讀任何產品程式碼，照操作說明從頭走一遍。
> **他找到十一個清單上沒有的問題，其中八個是「文件說了一件頁面上不成立的事」。**
>
> 最難堪的是：上一節說第三族的三個「都已修」，其中一個的修法是「示範用標記指認，不要用位置」。
> **審查者去數了：整張表格 0 列帶那個標記。** 我們修好了一句過期的話，換上另一句過期的話。
>
> 他也推翻了我們自己寫下的兩個結論：我們說中文任務「能力面沒有做」，他實測拿到 `T-DECLARED`
> 並通過驗證；我們說「不乾淨的安靜結果」徽章在部署上一個實例都找不到，他找到了。
> **兩個都是朝對我們不利的方向過期**——我們把自己講得比實際差。
>
> **一個看起來合理而錯誤的說法，比一次大聲的失敗嚴重得多。** 我們把這條標準嚴格地套在自己的
> 執行結果上，卻沒有套在自己的操作手冊上。
>
> 那五個要動到頁面的已經修完（第 16–20 筆）。修法的共通規則只有一條：
> **一個關於產品的句子，它的測試必須拿渲染出來的頁面去比對程式碼推導出來的值，
> 不能比對那句話本身**——前三個之所以能在一次專門在找它們的審查裡活下來，正是因為
> 附近的測試只釘住了散文。

### 十、第二輪：修那十一項的過程，暴露出一個比它們都嚴重的（第 21 筆，尚未修完）

**（這一節同樣是給評分者的說明裡新增的。這是目前唯一一個會產生錯誤答案、
而不只是錯誤句子的缺陷，請當成這一輪的重點查證對象。）**

> OP-5 的兩個標準案例（`eval/dev-set.md` DEV-04 / DEV-05）問的是展開後才看得到的**值**。
> 系統凍結下來的條件只宣稱「這個box 不再是摺疊的」，然後回傳 `succeeded_verified`、算成功。
> **問的東西一個都沒答，而答案就在存檔的位元組裡。**
>
> 原因不是讀不到：OP-5 的值宣稱只在正規表達式匹配到**具名** row group 時才建立，
> 而兩個標準案例問的都是**序數**的。
>
> 真正的問題在下一層。這條規則早就存在——A25.3：「一個任務問了 n 件事，就要產生 n 個宣稱，
> 否則就是 `partial`」——它有實作、有跨十一種句型的參數化測試，**而它只被接到一個呼叫端**。
> 其他編譯凍結條件的地方，從來沒有人拿這條規則去稽核過。驗證層救不了：它比對的是
> 「宣稱 vs 存檔」，看不到任務原文。
>
> **這是缺陷家族的新品種：一個本身完全正確、而且有測試的檢查，被接到了它該管的其中一個地方。**
> 裁決在 Amendment 28，驗收項 A-84 / A-85。在修完之前，所有公布 OP-5 結果的地方
> 都改成說明它量的是**狀態轉換**而不是值。

---

## 數字一覽

| | |
|---|---|
| 線上 | <https://wf-agent.zeabur.app> |
| 承諾的操作 | 4 項，在 2 個我們不控制的網站上 |
| 自建開發題組 | 15 題；計入通過率的 11 題中通過 10 題 |
| 從未碰過的網站題組 | 10 題；通過驗證 3/10，95% 區間 0.11–0.60 |
| **保留題組（只跑一次）** | **8 題；1/8，95% 區間 0.02–0.47**；承諾層 4 題中 1 題，區間 0.05–0.70 |
| 保留題組沒開始瀏覽的 | 8 題中 5 題：3 題是一次機器人規則檔取得逾時、2 題是任務沒指名頁面 |
| 延遲中位數 | 不用模型 0.16 秒 · 承諾層 5.85 秒 · 實驗層 12.18 秒 |
| 成本 | 每次用到模型的執行中位數 **USD 0.0019**；整個開發累計實付 **USD 0.1515**（記在持有付費金鑰的計分服務上；公開站是另一本帳，所以 `/healthz` 的 `cumulative_billed_usd` 是別的數字） |
| 記憶體 | 峰值 2,320 MB / 4 GB，同時只跑 2 筆——**記憶體才是這個系統的瓶頸，不是 CPU 也不是錢** |
| 測試 | 638 個通過 |
| 第二個題目 | 未實作，只有設計好的介面契約 |

---
---

# 我們已知的問題

> ## ⛔ 請先做完你自己那一遍，再讀這一節。
>
> 這一節會告訴你我們自己已經知道的那些事（第十節是最新、也最嚴重的一個）。**如果你先讀它，你會不自覺地只去找這幾樣，
> 而錯過我們沒想到的那些。**
>
> 讀完之後，**請回報你找到而這份清單上沒有的問題——那些才是有價值的發現。**
>
> 其中第 1 條和第 3 條在這份文件寫作期間就被修好並上線了。我們沒有把它們從清單上拿掉，
> 一來是因為它們的成因值得你知道，二來是因為它們現在變成**可以反過來驗的項目**：
> 我們說修好了，你去確認是不是真的。

---

**1. 執行中的進度顯示曾經會永久卡住——這一條在這份文件寫作期間修好並上線了。**
進度是靠一條即時串流推的，而串流中斷時沒有任何後備。連線一斷（我們自己換容器時就會有
十幾二十秒的斷線），頁面就停在最後一行進度上繼續轉，**而那一次執行其實早就成功了**。
我們自己的介面，對我們自己的執行狀態，給了一個看起來合理而錯誤的說法。

現在的行為：串流斷掉時會**明講**「即時更新已關閉，改為每三秒輪詢」，另有二十秒的
靜默偵測（串流沒斷但停止推送，畫面上和斷線長得一模一樣），滿五分鐘就停下來請你
重新整理，而不是無限轉。**無聲地降級只是把同一個缺陷換一件安靜的外衣。**

**所以這一條反過來變成你的查證項**：送出一筆會真的瀏覽的任務，在它執行中把網路斷開
幾秒再接回來（或直接觀察頁面在斷線後的行為）。你應該看到一句明確的降級公告，
而不是一個安靜地繼續轉的圈。看不到就是我們沒修好。

**2. 首頁「承諾操作」那一排，其中一顆範例任務的文字沒有指名條目。**
就是第二顆（`Expand the collapsed navbox on that article…`）。「that article」沒有指名是哪一篇，
所以它會被「必須指名頁面」那條規則正確地拒絕。**行為對，示範壞掉**——
一顆用來展示第二項承諾的按鈕，永遠展示不出第二項承諾。

上一輪審查者由此推得一個更重的結論：**四項承諾裡的第二項，在整個部署上找不到任何一個
成功實例，所以評分者能自行驗證的只有 3/4。** 我們的回應是把一句實測可用的任務原文
直接寫進給評分者的說明（`On the Wikipedia article for Apple Inc., expand the first
collapsed box…`），並在旁邊明說那顆按鈕一定會被拒。**按鈕本身還沒改。**
請確認那句原文真的會成功，並確認說明有把這個缺口講出來、而不是掩蓋掉。

**3. 限制清單的可執行檢查——這一條在這份文件寫作期間補跑了，但那份報告的結論已被推翻。**
支援頁上那七條限制，我們承諾「送審前要對線上系統重跑一次」。那次重跑的報告
（`eval/results/limitations-371ba697fa35.json`）說七條全部重現、沒有一條需要改。
**上一輪審查者逐條手動比對，找到一條不重現**：L-1 補救版公告的 `failure_class` 與實際跑出來的
不同。也就是說，**那支自動檢查程式比對的粒度沒有涵蓋 `failure_class`**——
一個「檢查得比它自己宣稱的鬆」的檢查，正是這份提交列為第一族的那種缺陷。
公告文字還沒改。

**這一條同樣反過來變成你的查證項**：你在步驟 8 做的逐條比對，就是這份報告的獨立重跑。
兩者對不上的地方，請直接回報——**我們的規則是「某條不重現時，缺陷是那個條目，不是系統」**，
所以對不上就是那份公告該改。

**4. 分層路由認得的講法，比支援矩陣承諾的窄。**
已量測、已公布：在完全沒看過的題組裡，出題者標為承諾層的四題，有兩題被路由到實驗層。
承諾寫的單位是「網站 × 操作」，實作認得的是一組比較窄的措辭。

**5. 模型憑空生出一個頁面元素參照時，會被歸類成我們自己的內部錯誤。**
分類不準確——那是模型的輸出問題，不是我們的程式炸了。已記錄，未修。

**6. 「被拒絕」這個標籤同時承載兩種意思。**
`robots_disallowed` 同時代表「站方禁止我們」和「我們讀不到站方的規則」。
兩種拒絕都是對的，但只有一個標籤是對的。**我們曾經因此誤讀自己的計分結果**
（詳見上面「還有缺陷的地方」第二點）。分辨方法只有一個：打開軌跡看 `source` 欄。

**7. 第二個題目與安全測試組刻意未實作。**
第二個題目只留下一份凍結的介面契約，後面什麼都沒有。
安全方面存在的是出口防護、機器人規則執行、拒絕分類法；不存在的是安全測試題組和注入偵測器。

---

**以下四條是上一輪審查者找出來的，全部是「頁面對自己的描述錯了」，全部還沒修。**
我們把它們寫在這裡，是因為它們現在是**你可以反過來驗的項目**——我們說還沒修，你去確認。
如果其中任何一條在你操作時已經修好了，請直接回報「已修」，那也是有效的發現。

**8. 首頁的 `pre-executed` 標籤，一列都渲染不出來。** 首頁那段說明宣稱可以靠標籤找到
預先執行的示範列、而且每一列會顯示證據擷取日期。**實際上 0 列有標籤、0 列有日期。**
成因是那張表「同一段任務只留最新一筆」，任何人重跑示範任務就會把示範列擠掉。
更難堪的是：這一條**本身就是我們上一次修「文件描述過期」時換上去的新說法**——
我們用一句過期的話換掉另一句過期的話，而且一樣沒有東西在檢查它。

**9. 首頁承諾的逃生口不存在。** 同一段說明寫「Every run is still listed at `/api/runs`」，
但 `GET /api/runs` 回 **HTTP 405**（那個位址只收 POST）。所以**沒有任何路徑可以看到完整的
執行清單**，被去重擠掉的列就真的看不到了。

**10. `/coverage` 有兩句自述說反了。** 該頁寫「只計算這個部署自上次重啟以來」以及
「這裡的儲存是短暫的，重新部署會重置這張表」。實際上這份帳本記在持久磁碟上，
**跨重啟、跨部署累計**。照那兩句讀，會把一份長長的「從未發生」清單誤解成「剛重啟而已」。

**11. `/support` 上 L-1 補救版公告的失敗原因是錯的。** 公告寫 `failed / budget_exhausted`，
實測是 `failed / verification_mismatch`。理由段落的敘述與軌跡相符，只有結論標籤錯。

---
---

# 請你回報的格式

請照這個形狀給我們，不要寫成散文。

## 第一部分：逐步的相符 / 不相符表

一列一個查證項目。用步驟編號加項目編號定位（例如「步驟 5 第 2 項」）。

| 位置 | 判定 | 我實際看到什麼 |
|---|---|---|
| 步驟 1 第 3 項 | 不相符 | 整張 Runs 表格只有 1 列有 `pre-executed` 標籤，0 列有 `evidence captured` 日期 |
| … | … | … |

**每一列的「我實際看到什麼」都要具體到可以被別人重現。**
判定只有三種：**相符** / **不相符** / **無法判斷**。

## 第二部分：你找到而「已知還沒修」清單上沒有的問題

**這一部分是這次審查最有價值的產出。** 每一項請寫：

- **現象**：你看到什麼（一句話）
- **重現方式**：開哪一頁、點什麼、看哪裡
- **為什麼這是問題**：它會讓評分者得出什麼錯誤結論
- **嚴重度**：高（會讓人相信一個不成立的宣稱）／中（會讓人困惑）／低（不好看但不會誤導）

**請特別注意「靜默失敗」類的問題**——一個看起來正常、實際上錯的畫面，
比一個明顯壞掉的畫面嚴重得多。這是這整份提交自稱在防的東西，
所以我們自己犯這個錯是最該被抓出來的。

## 第三部分：對這兩份報告的具體改寫建議

分三類寫，每一類都要**引用原句**，不要泛泛而談：

**（a）哪一句話會誤導。**
格式：「原句：『…』→ 問題：… → 建議改成：『…』」

**（b）哪裡該補而沒補。**
格式：「讀者在讀到 X 的時候會問 Y，但兩份文件都沒有回答。建議加在 Z 之後。」

**（c）哪裡太長、哪裡順序不對。**
格式：「第 N 節有 M 段在講同一件事，建議砍到 K 段」或「A 節應該放在 B 節之前，因為…」

最後請給**一句話總評**：如果你是評分者，讀完加操作完，你對這個系統最主要的印象是什麼？
**那個印象是我們想給的嗎？**

---
---

# 附錄

**這一節全部是選讀。** 上面那九步加兩節總結，已經足夠讓你完成全部三件工作。
下面這些是給「想再往下挖」的人用的，不打開它們你的審查也是完整的。

## A. 這個 repo 的目錄結構

```
wf-agent/
├── README.md                    整個系統的完整說明（英文，論證最完整的一份）
├── README.zh-TW.md              上面那份的中文精簡版
│
├── app/                         系統本體
│   ├── postcondition.py         把任務編譯成「可檢查的條件」並凍結
│   ├── planner.py               決定下一步做什麼（模型只在這裡與答案提議時出現）
│   ├── executor.py              真的開瀏覽器、真的操作頁面、保存頁面狀態
│   ├── verifier.py              裁決者：在保存的位元組裡重新找出答案
│   ├── memory.py                自我維護：只從「已驗證成功」的執行學定位方式
│   ├── suspicion.py             稽核安靜的結果（棄權是真的棄權，還是我們自己弄丟了元素）
│   ├── limitations.py           那七條限制的原文，就是支援頁渲染的來源
│   ├── demo.py                  首頁那九顆範例按鈕的原文
│   ├── robots.py / egress.py    政策邊界：機器人規則、私有網段拒絕
│   └── templates/               你在瀏覽器上看到的每一頁
│
├── fixture/                     我們自己寫的網站
│                                （用來重現困難互動，不計入任何成功率——自己出的考卷不算分）
│
├── eval/                        評測
│   ├── dev-set.md               開發題組 15 題
│   ├── experimental-set.md      從未碰過的網站 10 題
│   ├── test-set.md              保留題組 8 題（跑完才公布）
│   ├── validation-set.md        刻意從未執行，公布以證明我們真的扣住了
│   ├── holdout-manifest.md      保留題組的雜湊——在跑之前就先記錄
│   ├── harness.py / oracles.py  計分程式，以及獨立推導正確答案的那一支
│   └── results/                 每一輪的成績檔 + bundles/ 六輪完整證據（含保留輪）
│
├── docs/                        報告與紀錄（見下）
├── prompts/                     兩個工作階段的逐字 prompt 紀錄
├── tests/                       638 個測試
└── deploy/ · Dockerfile         部署
```

## B. 哪一份報告回答哪一個問題

| 你想知道 | 看這份 |
|---|---|
| **這個系統怎麼用**（給評分者的入口） | `docs/grader-guide.zh-TW.md` |
| **這一份**（給最終審查者，含查證清單） | `final-reviewer-brief.zh-TW.md` |
| 更長的網站操作手冊 | `docs/user-guide.zh-TW.md` |
| **效能、成本、擴展性、正確性怎麼驗證** | `docs/analysis-report.md`（中文精簡版：`docs/analysis-report.zh-TW.md`） |
| 系統完整說明、設計決策、AI 幫上什麼忙 | `README.md`（中文精簡版：`README.zh-TW.md`） |
| **整個開發過程、遇到什麼困難、怎麼改的** | `docs/project-report.zh-TW.md` |
| 花了多少錢（唯一的總額來源，程式產生） | `docs/spend-ledger.md` |
| 保留題組的雜湊是不是真的先記錄的 | `eval/holdout-manifest.md`（附自己驗證的指令） |

## C. 如果你想追我們是怎麼想的

這幾份不是給評分用的，是給「想知道這些決定怎麼發生」的人看的。

| | |
|---|---|
| `docs/task1-discovery.md` | 最初的探索推理，**刻意從未更新**——它保留了我們一開始想錯的地方 |
| `docs/task1-spec.md` | 工程規格，以及一連串書面修訂。**每一條修訂都附著造成它的那個缺陷**，這是完整的推理軌跡 |
| `docs/engineering-brief.md` | 給實作方的工作規則。最重要的一條是：規格擋路時要停下來爭論，不准安靜地重新詮釋 |
| `prompts/` | 兩個工作階段的每一則 prompt，逐字、未整理 |
| `docs/task2-seam.md` | 第二個題目的介面契約——**設計完成，刻意不實作**，並說明為什麼 |
| `docs/m4-fail-closed-inventory.md` | 每一個「出錯時會怎樣」的清單：是安靜地錯，還是大聲地停 |
| `docs/m8-quiet-failures.md` · `m8-credential-exposure.md` | 安靜失敗與憑證外洩的專項檢查 |

## D. 如果你也能跑指令

```bash
pytest                              # 638 個測試
python -m eval.harness --split dev  # 我們的開發題組
python -m eval.limitations_check --base-url https://wf-agent.zeabur.app
                                    # 把「已知會失敗的那些任務」逐一對線上系統跑一次
shasum -a 256 eval/test-set.md      # 應與 eval/holdout-manifest.md 裡的雜湊相符
```

最後那兩條是重點：**限制清單是可以執行的，不是寫出來的**；
而保留題組的雜湊在跑之前就已經記錄，所以成績、題目檔、雜湊三者構成一個不需要相信我們的閉環。

**但這四行只是加分項。** 你在瀏覽器上做的那一遍，才是我們真正需要的東西——
因為那是評分者會做的事，而我們已經證明過一次：這個專案最嚴重的兩個缺陷，
都是靠「有人真的去點開一頁」才找到的，不是靠任何測試。
