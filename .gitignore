# Credentials — never committed, never printed, never logged
api_keys/
*.key
.env
.env.*

# Test spec — kept local, this repo is public
task_description/

.DS_Store

# Python virtualenv
.venv/
__pycache__/
*.pyc
